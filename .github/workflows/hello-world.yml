name: hello-world
on:
  workflow_call:
    # inputs:
    #   example-input:
    #     required: true
    #     type: string
    # secrets:
    #   example-secret:
    #     required: true
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: Hello-World
        run: echo "Hello World!"